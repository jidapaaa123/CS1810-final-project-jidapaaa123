services:
  api:
    build: ./api
    ports:
      - "8080:8080"
    volumes:
      - ./api/database:/app/data
    restart: always

  frontend:
    build: ./docs
    ports:
      - "80:80"  
    restart: always
    depends_on:
      - api